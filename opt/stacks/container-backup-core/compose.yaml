services:
  container-backup-core:
    image: zackwag/flask-container-backup:latest
    container_name: container-backup-core
    restart: unless-stopped
    ports:
      - 2128:2128
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock:ro
      - /docker/container-backup-core/config:/app/config
      - /docker:/source
      - onedrive:/destination
    environment:
      - PYTHONUNBUFFERED=1
      - TZ=America/New_York
      - MODE=hub
networks: {}
volumes:
  onedrive:
    driver: rclone
    driver_opts:
      remote: Onedrive:/backup
